// Shadows themes/blowfish/assets/js/page.js (Firebase views/likes tracking).
// This site doesn't use Firebase, so the original script is a no-op anyway —
// shadowing with an empty file ships a 0-byte fingerprinted script.
